---
layout: post
title: ssh키를 통해 gcp
color: rgb(242,85,44)
tags: [gcp, firebase]
---

Google Cloud 콘솔 또는 Google Cloud CLI를 사용하여 VM에 연결하면 Compute Engine이 SSH 키를 자동으로 만듭니다. Compute Engine에서 키를 구성하고 저장하는 방법에 대한 자세한 내용은 SSH 연결 정보를 참조하세요.

타사 도구 또는 OpenSSH를 사용하여 VM에 연결하는 경우 VM에 키를 추가해야 연결할 수 있습니다.

1. 로컬에서 ssh키 생성

2. gcp에 ssh키 등록
3. 인스턴스 접속

특정 호스트에 최초로 ssh 접속 시 서버의 공개키를 받아올 지 묻고, 이를 기반으로 하는 fingerprint가 생성되어 ~/.ssh/known_hosts 파일에 해당 RSA key가 등록되어 다음 접속부터는 물어보지 않는다.
